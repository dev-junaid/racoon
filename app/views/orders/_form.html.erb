<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="field">
    <%= form.label :customer_emails %>
    <%= form.text_field :customer_emails %>
  </div>

  <div class="field">
    <%= form.label :customer_phone %>
    <%= form.text_field :customer_phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
